angular
.module('mainApp')
.factory('Factory', Factory);

Factory.$inject = ['$http'];

// set up the services needed for this factory
function Factory($http){

	var service = {
		
	};

	return service;

	////////////////////
	
}